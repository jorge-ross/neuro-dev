import styled from '@emotion/styled';

const NavBarContainer = styled.div`
display: flex;
height: 64px;
padding: 10px 30px;
`

function Header() {
  return (
    <NavBarContainer>
      
    </NavBarContainer>
  )
}

export default Header;